services:
  - type: web
    name: weather-app
    env: docker
    buildCommand: docker build -t weather-app .
    startCommand: docker run -d -p 7000:7000 weather-app
    plan: free
    branch: main
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
